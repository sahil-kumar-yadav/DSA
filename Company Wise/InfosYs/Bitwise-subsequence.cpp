#include <iostream>
#include <vector>

using namespace std;

void findMaxiSub(int arr[], int n, vector<int> &ans)
{

    // include exclude pattern lagana hai
}

int main()
{
    int arr[] = {15, 6, 5, 12, 1};

    int n = 5;
    vector<int> ans;
    findMaxiSub(arr, n, ans);

    return 0;
}